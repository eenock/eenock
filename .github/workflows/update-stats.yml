name: Update Profile Stats

on:
  schedule:
    - cron: '0 0 * * *'   # daily
  workflow_dispatch:

jobs:
  update-stats:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Fetch top-langs SVG
        run: |
          mkdir -p assets
          urls=(
            "https://vercel.app"
            "https://vercel.app"
          )
          for url in "${urls[@]}"; do
            echo "Trying: $url"
            if curl -fsSL "$url" -o assets/top-langs.svg && [ "$(wc -c < assets/top-langs.svg)" -ge 500 ]; then
              echo "Success: $url"
              break
            fi
          done

      # NEW STEP: Clear any blocking directory or broken artifact named trophy.svg
      - name: Clean conflicting files
        run: rm -rf trophy.svg

      - name: Fetch trophies SVG
        uses: ryo-ma/github-profile-trophy@master
        with:
          username: eenock
          theme: tokyonight
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Move Trophy file
        run: |
          mkdir -p assets
          if [ -f "trophy.svg" ]; then
            mv trophy.svg assets/trophies.svg
          fi

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@://github.com"
          git add assets/*.svg
          git diff --staged --quiet || git commit -m "chore: update profile stats"
          git push
